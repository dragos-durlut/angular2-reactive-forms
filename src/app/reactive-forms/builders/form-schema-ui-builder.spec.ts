import { FormSchemaUiBuilder } from './form-schema-ui-builder';

describe('FormSchemaUiBuilder', () => {
  it('should create an instance', () => {
    expect(new FormSchemaUiBuilder()).toBeTruthy();
  });
});
