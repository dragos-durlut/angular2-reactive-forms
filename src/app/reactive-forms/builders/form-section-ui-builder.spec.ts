import { FormSectionUiBuilder } from './form-section-ui-builder';

describe('FormSectionUiBuilder', () => {
  it('should create an instance', () => {
    expect(new FormSectionUiBuilder()).toBeTruthy();
  });
});
