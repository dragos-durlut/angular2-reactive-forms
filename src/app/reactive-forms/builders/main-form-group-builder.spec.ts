import { MainFormGroupBuilder } from './main-form-group-builder';

describe('MainFormGroupBuilder', () => {
  it('should create an instance', () => {
    expect(new MainFormGroupBuilder()).toBeTruthy();
  });
});
