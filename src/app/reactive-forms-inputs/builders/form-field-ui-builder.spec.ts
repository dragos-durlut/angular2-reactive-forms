import { FormFieldUiBuilder } from './form-field-ui-builder';

describe('FormFieldUiBuilder', () => {
  it('should create an instance', () => {
    expect(new FormFieldUiBuilder()).toBeTruthy();
  });
});
