import { FormControlBuilder } from './form-control-builder';

describe('FormControlBuilder', () => {
  it('should create an instance', () => {
    expect(new FormControlBuilder()).toBeTruthy();
  });
});
