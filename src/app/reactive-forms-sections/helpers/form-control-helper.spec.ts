import { FormControlHelper } from './form-control-helper';

describe('FormControlHelper', () => {
  it('should create an instance', () => {
    expect(new FormControlHelper()).toBeTruthy();
  });
});
